# Runtime and test dependencies for this repository.
#
# The type checkers the benchmarks *measure* are installed separately, at
# their current versions, by the benchmark workflows. They are deliberately
# not pinned here.
pytest>=6.0
pyrefly
PyYAML>=6.0
